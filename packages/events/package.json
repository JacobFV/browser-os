{
  "name": "@browser-os/events",
  "version": "0.1.0",
  "description": "Event bus system for IPC and pub/sub",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@browser-os/schemas": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}

