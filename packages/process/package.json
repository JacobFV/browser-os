{
  "name": "@browser-os/process",
  "version": "0.1.0",
  "private": true,
  "description": "Process management for browser-os",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "lint": "eslint src --ext ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@browser-os/core": "workspace:*",
    "xstate": "^5.0.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.3.0"
  }
}

