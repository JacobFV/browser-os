{
  "name": "@browser-os/app-sdk",
  "version": "0.1.0",
  "private": true,
  "description": "App SDK for browser-os",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "lint": "eslint src --ext ts",
    "test": "vitest --run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@browser-os/core": "workspace:*",
    "@browser-os/process": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.0.0",
    "react": "^18.2.0",
    "tsup": "^8.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}

