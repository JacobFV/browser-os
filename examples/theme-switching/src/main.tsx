import React from 'react';
import ReactDOM from 'react-dom/client';
import { ThemeSwitchingExample } from './App';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ThemeSwitchingExample />
  </React.StrictMode>,
);

