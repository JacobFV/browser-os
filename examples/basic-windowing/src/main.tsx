import React from 'react';
import ReactDOM from 'react-dom/client';
import { BasicWindowingExample } from './App';
import './App.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BasicWindowingExample />
  </React.StrictMode>,
);

