import React from 'react';
import ReactDOM from 'react-dom/client';
import { CustomAppExample } from './App';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <CustomAppExample />
  </React.StrictMode>,
);

