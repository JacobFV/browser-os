import React from 'react';
import ReactDOM from 'react-dom/client';
import { WordProcessorApp } from './App';
import './App.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <WordProcessorApp />
  </React.StrictMode>,
);

