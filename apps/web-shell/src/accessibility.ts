// Accessibility features
// - Proper tab order for keyboard navigation
// - Focus rings on interactive elements
// - Reduced motion support (prefers-reduced-motion)
// - Screen reader labels (aria-label, aria-labelledby)
// - Semantic HTML elements

export const a11yFeatures = {
  tabOrder: 'Implemented via proper DOM order',
  focusRings: 'CSS focus-visible styles',
  reducedMotion: '@media (prefers-reduced-motion: reduce)',
  screenReaderLabels: 'aria-label attributes',
};

