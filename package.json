{
  "name": "browser-os",
  "version": "0.1.0",
  "private": true,
  "description": "A monorepo for browser-based operating system UI components",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean && pnpm store prune"
  },
  "keywords": [
    "monorepo",
    "react",
    "component-library",
    "browser-os",
    "ui"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "turbo": "^2.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}

